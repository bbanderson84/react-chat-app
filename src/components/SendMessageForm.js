import React from 'react'

const SendMessageForm = props => (
    <form className="sned-message-form">
        <input 
            placeholder="SendMessageForm"
            type="text" />

    </form>
)

export default SendMessageForm
